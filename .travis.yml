sudo: false
language: java

services:
  - mysql

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

before_install:
  - mysql -e "create database IF NOT EXISTS bubbles;" -uroot

jdk:
  - oraclejdk8

before_install:
  - export TZ=Asia/Shanghai

